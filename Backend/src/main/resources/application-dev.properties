# -------------------------
# Development Profile
# -------------------------

# Database - Local MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/elitehaven?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=God@2004

# Show SQL for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.hms.elite_haven=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Initialize with data.sql
spring.sql.init.mode=always
